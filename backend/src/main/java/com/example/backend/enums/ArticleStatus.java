package com.example.backend.enums;

public enum ArticleStatus {
    DRAFT,      // Bản nháp (Đang viết dở, chưa hiện lên web)
    PUBLISHED,  // Đã xuất bản (Hiện trên web)
    HIDDEN      // Đã ẩn (Lưu trữ, không hiện nữa)
}