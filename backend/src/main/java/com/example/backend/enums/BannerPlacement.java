package com.example.backend.enums;

public enum BannerPlacement {
    HOME_MAIN_SLIDER,      // Slide to và chạy tự động ở chính giữa trang chủ
    HOME_SUB_BANNER,       // 2-3 banner tĩnh nhỏ nằm bên phải hoặc bên dưới slider chính
    CATEGORY_LAPTOP,       // Banner nằm trên cùng khi khách vào danh mục "Laptop"
    CATEGORY_SMARTPHONE,   // Banner nằm trên cùng khi khách vào danh mục "Điện thoại"
    POPUP_PROMO            // Popup quảng cáo nhảy ra giữa màn hình khi mới vào Web
}